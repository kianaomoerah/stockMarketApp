<div class="container mt-5">
  <div class="row d-flex justify-content-center">
    <div class="card detail-card col-sm-8 col-md-6 col-lg-4 col-xl-3">
      <%= image_tag(profile["logo"], alt: "Company logo for #{profile["name"]}", :class => "details-img") %>
      <div class="card-body text-center">
        <h5 class="card-title"><%= profile["ticker"] %></h5>
      </div>
    </div>
    <div class="card detail-card col-sm-8 col-md-6 col-lg-4 col-xl-3 d-flex justify-content-center text-center">
      <div class="row">
        <table class="table">
        <thead colspan="2" style="font"><h5><%= profile["name"] %> Details</h5></thead>
        <tbody>
          <tr>
            <td>Current Price:</td>
            <td>$<%= quote["c"] %></td>
          </tr>
          <tr>
            <td>Previous Close:</td>
            <td>$<%= quote["pc"]%></td>
          </tr>
          <tr>
            <% sign = quote["dp"] > 0 ? "+" : "" %>  
            <td>Change (%)</td>
            <td><%= sign %><%= precision(quote["dp"]) %>%</td>
          </tr>
          <tr>
            <td>Today's High:</td>
            <td>$<%= precision(quote["h"])%></td>
          </tr>
          <tr>
            <td>Today's Low</td>
            <td>$<%= precision(quote["l"])%></td>
          </tr>
          <tr>
            <td>52 Week High:</td>
            <td>$<%= precision(metric["metric"]["52WeekHigh"])%></td>
          </tr>
          <tr>
            <td>52 Week Low:</td>
            <td>$<%= precision(metric["metric"]["52WeekLow"])%></td>
          </tr>
        </tbody>
      </table>
      </div>
    </div>
  </div>
</div>