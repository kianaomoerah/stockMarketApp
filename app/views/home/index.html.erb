<div class="jumbotron p-3">
  <h1 class="display-3 font-weight-bold">TradePal</h1>
  <p class="lead">Stock tracking made easy! TradePal helps you to find and view company stock information within the US market. Save up to eight stocks to your portfolio for easy viewing and tracking.</p>
  <hr class="my-4">

  <% if !user_signed_in? %>
    <p class="lead">Log in or sign up to start adding stocks to your tracker portfolio: </p>
    <div class="d-inline p-2">
      <%= link_to "Sign Up", new_user_registration_path, class: "btn btn-primary btn-lg" %>
    </div>
    <div class="d-inline p-2">
      <%= link_to "Log In", new_user_session_path, class: "btn btn-primary btn-lg" %>
    </div>
  <% else %>
    <p class="lead">Welcome back! View your portfolio or search for new stocks:</p>
    <div class="d-inline p-2">
      <%= link_to "My Stocks", stocks_path, class: "btn btn-primary btn-lg" %>
    </div>
    <div class="d-inline p-2">
      <%= link_to "Search Stocks", new_stock_path, class: "btn btn-primary btn-lg" %>
    </div>
  <% end %>
</div>